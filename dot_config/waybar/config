{
    "layer": "top",
    "position": "top",
    "height": 32,
    
    "modules-left": ["hyprland/workspaces", "hyprland/window"],
    "modules-center": ["custom/media"],
    "modules-right": ["tray", "custom/cpu-load", "custom/mem-load", "custom/tempcpu", "custom/tempgpu", "pulseaudio", "hyprland/language", "clock"],
    
    "hyprland/window": {
        "icon": true
    },
    
    "pulseaudio": {
        "on-click": "pwvucontrol",
	"format": "{icon}",
	"format-icons": [ "󰕿","󰖀","󰕾","󱄡" ]
    },

    "custom/media": {
        "exec": "playerctl -a metadata --format '{\"text\": \"{{markup_escape(artist)}} - {{markup_escape(title)}}\", \"tooltip\": \"{{playerName}}\", \"class\": \"{{status}}\", \"alt\": \"{{status}}\"}' -F 2>/dev/null",
        "return-type": "json",
        "format": "{icon} {text}",
        "format-icons": {
            "Playing": "",
            "Paused": "",
            "Stopped": ""
        },
        "max-length": 50,
        "on-click": "playerctl play-pause",
        "on-click-right": "playerctl next",
        "on-click-middle": "playerctl previous",
        "tooltip": true
    },

    "cpu": {
    	"format": "{usage}",
	"interval": 1
    },
    
    "custom/cpu-load": {
        "exec": "/home/lw/.config/waybar/cpu-load.sh",
        "interval": 1,
        "format": "{}",
        "return-type": "json"
    },

    "custom/mem-load": {
        "exec": "/home/lw/.config/waybar/mem-load.sh",
        "interval": 1,
        "format": "{}",
        "return-type": "json"
    },

    "custom/tempcpu": {
	"exec": "/home/lw/.config/waybar/temp-cpu.sh",
	"interval": 1,
	"format": "{}",
	"return-type": "json"
    },

    "custom/tempgpu": {
	"exec": "/home/lw/.config/waybar/temp-gpu.sh",
	"interval": 1,
	"format": "{}",
	"return-type": "json"
    },

    "memory": {
        "interval": 1,
        "format": "Mem: {used:>4.1f} Swp: {swapUsed:>4.1f}"
    },
    
    "hyprland/language": {
        "format": "{short:>}"
    },

    "tray": {
    	"icon-size": 16,
	"spacing": 20
    },

    "clock": {
        "format": "{:%a %d/%m %H:%M}",
        "tooltip-format": "<tt>{calendar}</tt>",
        "calendar": {
            "mode": "month"
        }
    }
}


