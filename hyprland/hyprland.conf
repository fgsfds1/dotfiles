# Common Hyprland configuration
# This file contains shared settings for all machines

# Environment variables
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = HYPRSHOT_DIR,/home/lw/Pictures/screenshots

# Electron scaling with hyprland
env = NIXOS_OZONE_WL,1

# Portal stuff
env = XDG_SESSION_TYPE,wayland

# Theme stuff
env = QT_QPA_PLATFORMTHEME,qt5ct
env = GTK_THEME,adw-gtk3-dark

# Which programs to use
env = TERMINAL,kitty
env = FILE_MANAGER,nautilus
env = MENU,rofi
env = BROWSER,firefox-developer-edition
env = LOCKER,hyprlock
env = EDITOR,nvim

# Other environment variables
# Tell GTK applications to use the portal
env = GTK_USE_PORTAL,1
# Don't know if this works, but let's try
env = QT_USE_PORTAL,1


# XWayland scaling
xwayland {
    force_zero_scaling = true
}

# Variables
$terminal = kitty
$fileManager = nautilus
$menu = rofi
$menuargs = -show drun -show-icons
$browser = firefox-developer-edition
$locker = hyprlock --immediate
$mainMod = SUPER
# Alt key as secondary modifier
$secondaryMod = ALT

source = ~/.config/hypr/colors.conf

# General settings
general {
    gaps_in = 3
    gaps_out = 6
    border_size = 3

    # Active window: warm orange gradient from your wallpaper
    col.active_border = $primary $primary_container 45deg
    # Inactive window: subtle outline color
    col.inactive_border = $outline_variant

    resize_on_border = true
    allow_tearing = true
    layout = dwindle
}

# removed stuff because deprecated or removed:
# dwindle.no_gaps_when_only = 1

# Dwindle layout
dwindle {
    pseudotile = true
    preserve_split = true
}

# Decorations
decoration {
    rounding = 8

    active_opacity = 1.0
    inactive_opacity = 0.9
    
    dim_inactive = true
    dim_strength = 0.1
    dim_special = 0.3

    shadow {
        enabled = true
        range = 6
        render_power = 3
        color = $shadow
        # Add some warmth to shadows using the surface container
        color_inactive = $surface_container_low
    }

    blur {
        enabled = true
        size = 5
        passes = 3
        vibrancy = 0.1696
        new_optimizations = true
    }
}

# Animations
animations {
    enabled = true

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 4, myBezier
    animation = windowsOut, 1, 4, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 4, default
    animation = workspaces, 1, 4, default
}

# Misc settings
misc {
    force_default_wallpaper = -1
    disable_hyprland_logo = false
    vrr = 2
}

# Input configuration
input {
    kb_layout = us, ru
    kb_options = grp:caps_toggle

    follow_mouse = 1
    sensitivity = 0

    repeat_rate = 80
    repeat_delay = 300

    touchpad {
    }
}

# Gestures
# gestures {
#     workspace_swipe = false
# }

# Per-device input configs
device {
    name = etps/2-elantech-touchpad
    enabled = false
}

device {
    name = logitech-gaming-mouse-g402
    accel_profile = flat
    sensitivity = -0.5
}

# Full-screen/TV workspace - minimal borders
workspace = w[tv1], gapsout:0, gapsin:0
workspace = f[1], gapsout:0, gapsin:0
windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
windowrule = rounding 0, floating:0, onworkspace:w[tv1]
windowrule = bordersize 0, floating:0, onworkspace:f[1]
windowrule = rounding 0, floating:0, onworkspace:f[1]

# Window management keybinds
# Q should close window gracefully
bind = $mainMod, Q, killactive
# Shift+Q should send sigkill
bind = $mainMod SHIFT, Q, exec, kill -KILL $(hyprctl activewindow -j | jq -r '.pid')
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, b, exec, $browser
bind = $mainMod, f, togglefloating,
bind = $mainMod SHIFT, f, fullscreen,
bind = $mainMod, P, pseudo,
bind = $mainMod CTRL SHIFT, K, exit

# Layout controls
bind = $mainMod, T, layoutmsg, togglesplit
bind = $mainMod CTRL, h, exec, hyprctl dispatch splitratio -0.1
bind = $mainMod CTRL, l, exec, hyprctl dispatch splitratio +0.1

# Focus movement
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Window movement
bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, l, movewindow, r
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, j, movewindow, d

# Workspace switching
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Use Alt-Tab to switch between recent workspaces
bind = $secondaryMod, Tab, workspace, prev

# Move windows to workspaces
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10

# Special workspace
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Mouse workspace switching
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Screenshot
bind = , PRINT, exec, hyprshot -m region

# Application launcher (why both)
bindr = $mainMod, R, exec, rofi -show run
bindr = $mainMod, Space, exec, $menu $menuargs

# Mouse binds
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Media and system controls
bindel = , XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.5 @DEFAULT_AUDIO_SINK@ 5%+
bindel = , XF86AudioLowerVolume, exec, wpctl set-volume -l 1.5 @DEFAULT_AUDIO_SINK@ 5%-
bindel = , XF86MonBrightnessUp, exec, brightnessctl set 5%+
bindel = , XF86MonBrightnessDown, exec, brightnessctl set 5%-
bindel = $mainMod, semicolon, exec, $locker
bindel = , switch:Lid Switch, exec, $locker

# Window rules
# Prevent maximization
windowrulev2 = suppressevent maximize, class:.*
# Prevent gnome filepicker dropping shadow
windowrule = noshadow, class:Xdg-desktop-portal-gtk
# Float (no tile) all filepickers
windowrule = float, class:Xdg-desktop-portal-gtk
# Make mpv always full-opacity
windowrule = opaque, title:(.*)( mpv)$
# Make mpv always full-brightness
windowrule = nodim, title:(.*)( mpv)$
windowrule = nodim, title:(.*)( mpv)$

# Color-based window rules using the matugen palette
# Error/critical applications get error colors
windowrulev2 = bordercolor $error $error_container, class:(firefox).*crashreporter
windowrulev2 = bordercolor $error $error_container, title:.*[Ee]rror.*
windowrulev2 = bordercolor $error $error_container, title:.*[Cc]rash.*

# Startup applications (common)
exec-once = swww-daemon
exec-once = nm-applet --indicator
exec-once = waybar
exec-once = dunst
exec-once = wl-paste --watch cliphist store

# GNOME integration
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = gsettings set org.gnome.desktop.interface gtk-theme 'adw-gtk3-dark'
exec-once = gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'

# XDG Desktop Portal integration
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# Load machine-specific configuration
source = ~/.config/hypr/host.conf

